EN:
🔹 POST 3 — DATA QUALITY (Real EDA, not just `.describe()`)

Data quality isn’t optional — it’s the foundation.

In STAGE 1 of the project, I ran a full data quality audit, going beyond traditional EDA.

Some of the validations performed:
- Correct data types for dates and metrics
- Detailed analysis of missing values (with context)
- Outlier detection with interpretation, not just statistics
- Logical validation of set order (`set_index`)
- Checks for unrealistic workout durations (sanity check)

Result:
- Only ~4 rows required manual correction
- Less than 1% of the dataset

All fixes were targeted, justified, and documented.

Conclusion:
- Statistical outliers ≠ data errors
- “Weird” data isn’t always wrong data

By the end of this stage, it was clear:
- The dataset is reliable
- It’s ready to be ingested into a database
- It doesn’t carry hidden technical debt

PT: 
🔹 POST 3 — QUALIDADE DE DADOS (EDA de verdade, não só .describe())

Data Quality não é opcional — é fundação

Na ETAPA 1 do projeto, fiz uma auditoria completa de qualidade dos dados, indo além da EDA tradicional.

Algumas validações realizadas:
- Tipagem correta de datas e métricas
- Análise detalhada de valores ausentes (com contexto)
- Detecção de outliers com interpretação, não só estatística
- Validação lógica de ordem das séries (set_index)
- Verificação de durações irreais de treino (sanity check)

Resultado:
- Apenas ~4 linhas precisaram de correção manual
- Menos de 1% do dataset

Todas as correções foram pontuais, justificadas e documentadas

Conclusão:
- Outliers estatísticos ≠ erros de dados
- Dados “estranhos” nem sempre são dados errados

Ao final dessa etapa, ficou claro:
- O dataset é confiável
- Está pronto para ingestão em banco
- Não carrega débito técnico oculto